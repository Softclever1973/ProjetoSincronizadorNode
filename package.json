{
  "name": "projeto-sincronizador-node",
  "version": "1.0.0",
  "description": "Servidor de sincronização Firebird matriz/filiais",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "client": "node src/client/index.js",
    "client:dev": "nodemon src/client/index.js",
    "generate-secret": "node -e \"process.stdout.write(require('crypto').randomBytes(64).toString('hex') + '\\n')\"",
    "build:server": "npx @yao-pkg/pkg --config package.json src/server.js --target node18-win-x64 --output dist/server.exe",
    "build:client": "npx @yao-pkg/pkg --config package.json src/client/index.js --target node18-win-x64 --output dist/client.exe"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^17.4.1",
    "ejs": "^5.0.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "node-firebird": "^1.1.5",
    "pg": "^8.20.0",
    "systray2": "^2.1.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "pkg": {
    "scripts": "src/**/*.js",
    "assets": [
      "src/client/views/**/*",
      "src/client/public/**/*",
      "src/client/assets/**/*",
      "src/client/assets/softcleverlogo.ico",
      "node_modules/systray2/traybin/**/*"
    ],
    "targets": [
      "node18-win-x64"
    ]
  },
  "pkg-exe-build": {
    "icon": "src/client/assets/softcleverlogo.ico"
  },
  "nodemonConfig": {
    "ignore": [
      "*.json",
      "*.ini",
      "conflitos.json",
      "tabelas-config.json"
    ]
  }
}
